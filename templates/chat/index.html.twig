<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chatbot</title>
        <script>
    function resetConversation() {
        document.getElementById('chat-container').innerHTML = '';
    }
</script>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: center;
            color: #ff7f0e; /* Orange */
            margin-bottom: 20px;
        }
        #chat-container {
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            background-color: #ffe4b5; /* Light orange */
            max-height: 300px;
            overflow-y: auto;
            margin-bottom: 20px;
        }
        .message {
            margin-bottom: 10px;
            padding: 8px 12px;
            background-color: #ffefd5; /* Pale orange */
            border-radius: 5px;
            overflow-wrap: break-word;
        }
        .message strong {
            color: #ff7f0e; /* Orange */
        }
        input[type="text"] {
            width: calc(100% - 70px);
            padding: 10px;
            border-radius: 5px 0 0 5px;
            border: 1px solid #ccc;
            font-size: 16px;
        }
        button[type="submit"] {
            width: 70px;
            padding: 10px;
            border: none;
            background-color: #ff7f0e; /* Orange */
            color: #fff;
            border-radius: 0 5px 5px 0;
            cursor: pointer;
            font-size: 16px;
        }
        button[type="submit"]:hover {
            background-color: #cc6600; /* Darker orange */
        }
        .btn-reset {
            display: block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #ff7f0e; /* Orange */
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            border: none;
            cursor: pointer;
            font-size: 16px;
            width: 100%;
        }
        .btn-reset:hover {
            background-color: #cc6600; /* Darker orange */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Chatbot</h1>
        <div id="chat-container">
            {% for message in chat_history|reverse %}
                <div class="message">
                    <strong>{{ message.author }} :</strong> {{ message.message }}
                </div>
            {% endfor %}
        </div>
        <form id="chat-form" action="{{ path('app_chat') }}" method="POST">
            <input type="text" name="user_message" id="user_message" placeholder="Votre message...">
            <button type="submit">Envoyer</button>
        </form>
        <button class="btn-reset" type="button" onclick="resetConversation()">Réinitialiser</button>
    </div>
</body>
</html>
