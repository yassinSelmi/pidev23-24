{% extends 'base.html.twig' %}

{% block title %}Nouvelle réservation d'événement{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-header bg-primary text-white">
                        <h1 class="mb-0">Nouvelle réservation d'événement</h1>
                    </div>
                    <div class="card-body">
                        {{ include('reservation_event/_form.html.twig') }}
                    </div>
                    <div class="card-footer bg-light border-top">
                        <a href="{{ path('app_reservation_event_index') }}" class="btn btn-secondary">Retour à la liste</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
