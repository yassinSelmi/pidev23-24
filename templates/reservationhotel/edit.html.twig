{% extends 'base.html.twig' %}

{% block title %}Edit Reservationhotel{% endblock %}

{% block body %}
    <h1>Edit Reservationhotel</h1>

    {{ include('reservationhotel/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_reservationhotel_index') }}">back to list</a>

    {{ include('reservationhotel/_delete_form.html.twig') }}
{% endblock %}
