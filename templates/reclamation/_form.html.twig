{{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}


        <div class="form-group">
            <label class="bmd-label-floating">   Contenu
            </label>
            <textarea type="text" class="form-control" {{ form_widget(form.contenu) }} </textarea>
            
            <div class="form-error">
                {{ form_errors(form.contenu) }}
            </div>
        </div>










        <div class="form-group">
            <label class="bmd-label-floating">   Date
            </label>
            <input value="2024-01-01" type="date" class="form-control" {{ form_widget(form.date) }}
        </div>





    {{ form_widget(form) }}
    <button class="btn btn-primary" class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
